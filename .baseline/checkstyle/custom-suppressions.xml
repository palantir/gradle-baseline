<?xml version="1.0"?>
<!DOCTYPE suppressions PUBLIC
    "-//Checkstyle//DTD SuppressionFilter Configuration 1.2//EN"
    "https://checkstyle.org/dtds/suppressions_1_2.dtd">

<!-- IMPORTANT ECLIPSE NOTE: If you change this file, you must restart Eclipse
 for your changes to take effect in its Checkstyle integration. -->
<suppressions>
    <!-- Do not lint class which is mostly copied -->
    <suppress files=".*StrictUnusedVariable.java" checks="." />
    <!-- The baseline-error-prone module is run within the compiler and can only use the main Java logger -->
    <suppress message="Use SLF4J instead of a logging framework directly" files="baseline-error-prone[/\\]src[/\\].*.java" />
</suppressions>
